import { ToggleSideBarDirective } from './toggle-side-bar.directive';

describe('ToggleSideBarDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleSideBarDirective();
    expect(directive).toBeTruthy();
  });
});
